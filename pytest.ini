[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: End-to-end pipeline tests (requires model files and network)
addopts = -v --tb=short
